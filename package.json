{
  "name": "repository-activity-card",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Generate a daily SVG card from a GitHub repository's previous-day activity.",
  "scripts": {
    "generate": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
